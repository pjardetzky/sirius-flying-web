This is the Sirius Flying, LLC web site source code.  It is based on Google App
Engine (Go) and Twitter Bootstrap user interface library.

The Google App Engine project name is 'siriusflying' and is contained in the
siriusflying subdirectory.  However, To prepare the APP under the Go framework,
you must first type:

> make

in the top level directory to unpack the Twitter Bootstrap library code into the
App Engine project directory.  Then the entire project can be uploaded to Google
via the GoogleAppEngineLauncher.

